import { SettingsPage } from '@pages/profile/settings';

// Блок 10: Settings переезжает с индекса на вложенный роут, функциональность та же
export default function ProfileSettingsScreen() {
  return <SettingsPage />;
}
