import { PlaceholderPage } from '@shared/ui';

export function ProductsPage() {
  return (
    <PlaceholderPage
      title="Товары"
      description="Таблица товаров с поиском и фильтрами, создание/редактирование, SKU и изображения (блоки W2–W3)."
    />
  );
}
